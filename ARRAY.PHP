   $options=$_POST['options'];
   for($i=0;$i<$count; $i++)
                        {
                            // $id= $options[$i];
                         
                          echo $globalListOptions[$i]->option_name;
                           //echo $options[$i];
                        }
  
